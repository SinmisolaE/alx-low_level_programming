#include <stdio.h>
/**
 * main - the main method
 * @argc: the number of aregument
 * @argv: array arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s", argv[0]);
	return (0);
}
